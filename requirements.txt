# GeoRes Core Dependencies
# Install PyTorch with CUDA support on Windows first if GPU is present:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126

torch>=2.0.0
torchvision>=0.15.0
pillow>=10.0.0
numpy>=1.26.0

# Optional / Data Ingestion (only needed to download EuroSAT dataset from Kaggle)
# kaggle>=1.6.0

# Development / Testing
pytest>=8.0.0
